# CPU-only deterministic HTML layout generation.
# Playwright 1.62.0 pins the compatible Chromium revision installed by
# python -m playwright install chromium.
numpy==2.1.3
Pillow==11.1.0
playwright==1.62.0
PyMuPDF==1.26.3
