# 注意力探针：阶段 0 接线、成本与变换校验

**日期**：2026-09-20
**前序**：`plans/LAYOUT_ATTENTION_TRACKING.md`（方案）、`docs/LAYOUT_ATTENTION_ROUTING_RESULT.md`（路由的判定）
**状态**：阶段 0 已完成（接线、成本、单步 eager 校验）。**阶段 1 未开始**——本文件的定位数字来自 4 页，是链路自检加初步读数，不是阶段 1 结论。

---

## 一、要回答的问题

模型自己的注意力里，有没有一个可读的「我现在读到哪一行」信号？

路由那轮（`bias2` 把 CER 从 0.1699 压到 0.1360）证明了「用真值字框给注意力加偏置」有效，但那一臂需要每步的真值字框，**不可部署**。可部署的版本得自己预测位置，而它只有在注意力本身带位置信号时才可能。本轮只观测、不干预，先把这个信号测出来。

失败模式不是「定位不准」，而是更根本的「**没有信号**」：若视觉总质量 $m_t$ 始终很低（模型主要靠语义 token 间注意力），定位从一开始就测不出来。这个判断要在阶段 1 早期做掉。

## 二、实验设置

| 项 | 值 |
|---|---|
| checkpoint | `glmocr_mthv2_sparse24_q32_layout_boxeq820_3000_from_boxeq58_a100_260916_v1/seed42/checkpoint-3000` |
| 数据 | MTHv2 sparse24 validation 的**前 4 页**（按真值长度分层等距抽取，短 77 字 / 长 335 字） |
| 分辨率 | `max_pixels=4000000`（2496 个视觉 token） |
| 臂 | `noroute`（不装探针）/ `probe_only`（只记录） |
| 探针层 | `0,4,8,12`，全部 16 个 query head |
| 入口 | `tools/training/run_glmocr_layout_attention_probe_a100.sh --stage 0` |

## 三、接线：探针是纯观测（判据通过）

两臂生成的 token **逐页逐位相同**：

| | noroute | probe_only |
|---|---|---|
| CER | 0.120126 | 0.120126 |
| sub/ins/del | 107 / 6 / 1 | 107 / 6 / 1 |
| 触顶 | 0 | 0 |
| 生成均值 | 283.8 | 283.8 |

探针不改序列、不改掩码、不改缓存。这条不通过，后面所有定位数字一律不读。1131 个解码步 / 4 页全部记录到，`grid_missing_steps=0`、`emitted_missing_steps=0`、`transform_failed={}`。

## 四、变换校验：重算的 logits 就是模型的 logits

这是本阶段唯一不可跳过的一项。探针自己重算 attention logits，它报的每个数都建立在「重算等于模型真实计算」之上——而**这是个测量，算错和算对长得一模一样**。

做法：用 `attn_implementation="eager"`（唯一会返回权重的路径）跑同一页的 prefill，把探针的 $m_t$ 与 $a^{\mathrm{vis}}$ 与模型自己的权重逐头、逐 query 位置比对。

**prefill 而非解码步**：探针的视觉 key 是在 prefill 捕获、之后每步复用的，所以变换必须正确的地方正是 prefill。

结果（bfloat16 权重 vs float32 重算）：

| 层 | $m_t$ 最大误差 | $m_t$ 平均误差 | $a^{\mathrm{vis}}$ 最大误差 | $a^{\mathrm{vis}}$ 平均误差 |
|---|---|---|---|---|
| 0 | 1.19e-2 | 6.9e-4 | 2.05e-2 | 2.8e-6 |
| 4 | 1.06e-2 | 1.0e-3 | 1.50e-2 | 3.0e-6 |
| 8 | 8.60e-3 | 1.1e-3 | 2.63e-2 | 2.8e-6 |
| 12 | 8.40e-3 | 8.3e-4 | 8.18e-3 | 3.4e-6 |

误差落在 bfloat16 的量化量级（相对精度约 0.4%，2496 个权重相加以致 $m_t$ 上约 1e-2），不是结构性差异——结构性错误（漏掉 RoPE、GQA 头映射错位、scale 缺失）会给出量级为 1 的差异。**校验通过**。复现：`tools/validate_attention_probe.py`。

### 4.1 两次失败记录（都是比对侧的错，不是变换的错）

第一次跑出 $m_t$ 误差 **1.0**，看起来像变换坏了，实际是**掩码被按行取错**：`_split_mask` 把掩码压平后只取第 0 行，这对解码步（q_len=1）正确，对 prefill 错误——因果掩码每个 query 位置一行，于是每个位置都拿「位置 0 的可见集合」去比。修正后保留行轴，返回可直接对 `[kv_heads, groups, q_len, keys]` 广播的形状。

**这条对真实运行没有影响**：解码步 q_len=1、本来就没有掩码（sdpa 对单 query 关掉 `is_causal`），全可见是对的。

校验器另有一个安全网：attention forward 经常以 `attention_mask=None` 调用、把因果交给 kernel，此时因果结构根本不存在于张量里。prefill 比对会因此把质量放到任何位置都看不到的 key 上，看起来就像变换坏了。校验器在这种情况下自行重建因果掩码，并记录 `mask_source`。

## 五、成本：超出预算（判据不通过）

| | noroute | probe_only（4 层 16 头） |
|---|---|---|
| 秒/页 | 15.97 | **35.32（+121.2%）** |
| 峰值显存 | 16627.6 MiB | 16627.6 MiB（+0） |

阶段 0 的工程目标是额外推理时延不超过 **20%**。4 层全开的成本是 **+121%**，远超。显存代价测不出（探针的张量很小，峰值由模型本身决定）。

探针的成本主要不在 GPU：每步每层每头都要在 Python 侧做归约（logsumexp、softmax、按行 index_add）并把结果序列化成 JSON——4 页产出 14 MB。**层数是线性因子**，所以减层是最直接的杠杆。

只留第 8 层的重跑见 §7。

## 六、信号：存在，而且集中在一层

72384 行 = 1131 步 × 4 层 × 16 头。

| 层 | $m_t$ | $lse_{vis}-lse_{text}$ | 行内最高质量 | 背景质量 | 归一化熵 |
|---|---|---|---|---|---|
| 0 | 0.107 | −4.29 | 0.076 | 0.549 | 0.739 |
| 4 | 0.175 | −2.31 | 0.125 | 0.363 | 0.862 |
| **8** | **0.408** | **−0.49** | **0.362** | 0.269 | 0.697 |
| 12 | 0.186 | −2.61 | 0.242 | 0.311 | 0.768 |

**第 8 层是看图的那一层**，与其余三层在量级上分开。

### 6.1 长度效应很大，裸 $m_t$ 不能直接判读

$m_t$ 是生成长度的函数：视觉 key 固定、文本 key 随生成增长，$m_t$ 单调下降。实测第 1 步平均 $m_t$ 0.374 vs 全步 0.221。记录里 $lse_{vis}$ 与 $lse_{text}$ **分开保存**就是为了这个——判读要用两者的差值或固定窗口，不能用裸 $m_t$。

### 6.2 「没有信号」这个失败模式没有被证实

$m_t$ 不接近零：15.1% 的头-行 $m_t>0.5$，第 8 层均值 0.408、logit 差 −0.49。方案 §4.2 的负面先验（来自写回诊断：decoder 不利用特征里的空间结构）**不排除**「模型自发注意力带空间定位」——这一轮的结果倾向于后者存在。

### 6.3 行读数本身是弥散的

`top_line_mass` 全体均值 0.201、背景质量均值 0.373，只有 11.6% 的头-行 `top_line_mass ≥ 0.5`。**单头的 argmax 是弱读数**，这正是方案里「先测各层各头的行概率、再估计行内位置」那条修订的理由：不能一上来就全头平均。相邻步保持同一 argmax 行占 66.6%（随机是 1/16≈6%），所以它远非随机，但每三步会跳一次。

## 七、只留第 8 层

两个理由指向同一件事：成本 +121% 里层数是线性因子，而选层本身也该选第 8 层。select 页上的**单头平均**行定位准确率：第 0 层 9.4%、第 4 层 22.7%、**第 8 层 80.0%**、第 12 层 59.0%（§8 的 `per_layer`）。注意这是「每头各自 argmax 的准确率对头取平均」，与 §8 表里聚合后的 96–98% 不是同一个统计量。

实测（`run_id` 尾号 `v4`，只装第 8 层）：

| | noroute | probe_only（1 层 16 头） |
|---|---|---|
| token 一致性 | — | **逐页逐位相同** |
| 秒/页 | 16.03 | 20.73（**+29.3%**） |
| 峰值显存 | 16627.6 MiB | 16627.6 MiB（+0） |
| 平均 $m_t$ | — | 0.408250 |

成本从 +121.2% 降到 +29.3%，**仍超出 20% 的预算**。$m_t$ 与 v3 分层表里第 8 层的 0.4082 完全一致——这是减层没有改变第 8 层读数的一致性自检。

剩下的 +4.7 s/页 全部来自 1 层 × 16 头的 Python 侧归约与 JSON 序列化。下一档杠杆是**减头**（按 select 页选少量固定头）或**降观测频率**，两者都按方案「先减少层/头或降观测频率」处理。

### 7.1 第 8 层不只是 m_t 高，读数也更集中、更稳

18096 行 = 1131 步 × 16 头。作为对照，4 层的对应值列在同列括号里。

| 量 | 第 8 层 | （4 层平均，§6） |
|---|---|---|
| $m_t$ 均值 | **0.4082** | 0.2190 |
| $m_t>0.5$ 的头-行占比 | **30.7%** | 15.1% |
| 第 1 步 $m_t$ vs 全步 | 0.6186 vs 0.4082 | 0.3735 vs 0.2190 |
| 行内最高质量均值 | **0.3622** | 0.2010 |
| `top_line_mass ≥ 0.5` 占比 | **26.1%** | 11.6% |
| `top_line_mass ≥ 0.2` 占比 | **68.6%** | 31.6% |
| 背景质量均值 | **0.2686** | 0.3727 |
| 相邻步保持同一 argmax 行 | **80.8%** | 66.6% |

随机基准是 1/16≈6%，所以 80.8% 的持续性是实打实的。第 8 层在质量、集中度、持续性三项上都与其余层分开。

## 八、离线定位评测（链路自检）

对齐链路首次在真实数据上跑通。自检 `pages_matching_prediction: 2/2`——按 step 拼回的文本必须是模型输出 prediction 的后缀（差值就是 prefill 那个 token 的贡献）。

按预登记纪律在 select 页选聚合方式、在 check 页报数。check 页（543 个可评分字，2 页）：

| 配置 | 聚合 | check 准确率 | 高置信覆盖 | 高置信准确率 | 保持上一行 | 均匀扫描 |
|---|---|---|---|---|---|---|
| 4 层 16 头 | 均值 | 96.3% | 0.0% | — | 54.8% | 75.9% |
| 4 层 16 头 | 逐头择优 | 98.5% | 95.2% | 98.8% | 54.8% | 75.9% |
| **仅第 8 层** | 均值 | 96.1% | 4.4% | 100% | 54.8% | 75.9% |
| **仅第 8 层** | 逐头择优 | **96.7%** | **89.1%** | **99.6%** | 54.8% | 75.9% |

两个基准都在同一批步上配对计算：**保持上一行**（阅读大体停在同一行，行切换少时这个先验很强）与**均匀扫描**（按恒定速率扫行）。

**减层几乎不损失准确率**：98.5% → 96.7%，差 1.8 个百分点 = 543 字里约 10 个字，在这个样本量上不构成差异（且方向上是第 8 层单独略低、4 层略高，量级在噪声内）。而成本从 +121.2% 降到 +29.3%。

逐头择优的置信度分箱单调（第 8 层 check 页：0.889 / 0.956 / 0.976 / 0.994）。**均值聚合的门槛不可用于覆盖度**：16 头平均把 `top_line_mass` 稀释到 0.2 量级，`confident_share` 只有 0.04–0.16，门槛因覆盖不足而不通过——这是门槛定义的缺陷，不是信号的问题。

**均值聚合的门槛不可用，这是门槛定义的缺陷**（4 层时 `confident_share=0`，第 8 层时 0.044）：16 头平均会把 `top_line_mass` 稀释到 0.2 量级，到不了 0.5，于是门槛无法判定（不是「失败」而是「测不了」）。下轮之前必须把置信度定义定死，并且只在 select 页上定。

### 8.1 单头之间存在巨大差异，这是减头的依据

第 8 层各头在 **select 页**上的行定位准确率（每头各自 argmax，402 步）：

| 头 | 准确率 | 头 | 准确率 |
|---|---|---|---|
| 10 | **96.3%** | 15 | 93.5% |
| 11 | **96.0%** | 3 | 93.3% |
| 2 | **96.0%** | 12 | 91.5% |
| 14 | 95.0% | 9 | 82.3% |
| 8 | 94.3% | 13 | 81.1% |
| 0 | 79.1% | 4 | 76.1% |
| 5 | 72.6% | 1 | 71.6% |
| 7 | 39.6% | 6 | **21.6%** |

跨度从 21.6% 到 96.3%。**8 个头在 93% 以上**（2/3/8/10/11/12/14/15），另外 8 个在 82% 以下。所以「只留少数固定头」既有依据也有收益：头数从 16 减到 8 应能把剩余的 +29.3% 压到约 +15%（预算内），而按上表它保留的是准确率 93% 以上的那一半。

**挑头的纪律**：上表是在 select 页上算的，check 页不参与挑头——这正是把开发子集分成两半的原因。挑头的依据只能是这张表，不能看 check 页的聚合结果。

## 九、本文件不下结论的地方

1. **4 页、945 个可评分字**。方案 §5 阶段 1 要求 24–32 页的开发子集。上表是链路自检加一个初步正向读数。
2. **真值行号是推断的**，来自编辑距离对齐后参考字符的框，不是直接真值。插入占 8%（48/591），歧义步 24 个，都已分开统计而不是摊进准确率。
3. **`noroute` 是 4 页基准**，与 149 页记录的 0.1699 不可直接比（页集不同）。这里的 0.1201 只用于两臂的逐位一致性判据。
4. 逐头择优是**按步选最自信的头**，其置信度是选择器的输出。它不使用真值，因此不是 oracle，但报告的是「选择器」的准确率，固定头的表现要另算。

## 十、下一步（尚未做）

阶段 0 的三项里，接线与变换校验已通过，**成本仍未达标（+29.3% vs 20%）**。所以下一步的顺序是：

1. **减头**：按 §8.1 的 select 页表固定 8 个头（2/3/8/10/11/12/14/15），重跑并复测成本与准确率。预计落到预算内，且保留的是 93% 以上的那一半。
2. **定死置信度定义**：在 select 页上定，避免 §8 那种「测不了」。定义之后再谈阶段 1 的门槛。
3. **阶段 1**：24–32 页开发子集，按方案 §5 报行定位准确率、置信度—准确率曲线、行内归一化误差、换行前后表现、漏读/重复附近的状态变化。

在没有完成 1 与 2 之前不要进阶段 1：现在的门槛定义测不了，而成本仍在预算外。

## 十一、依据

1. 方案与判据：`plans/LAYOUT_ATTENTION_TRACKING.md`，§4.2（观测必须测对）、§5（阶段 0–1）、§6（效果判定）。
2. 前序结果：`docs/LAYOUT_ATTENTION_ROUTING_RESULT.md`（路由有效、剂量-反应是陡崖、尚不可部署）；`docs/LAYOUT_BRANCH_WRITEBACK_DIAGNOSIS.md`（特征注入无效的负面先验）。
3. 实机接口核对：transformers 5.3.0 `GlmOcrTextAttention`——`forward(hidden_states, position_embeddings, attention_mask, past_key_values, cache_position, **kwargs)`，无 `rotary_emb` 属性、无 `position_ids` 参数，RoPE 只经 `position_embeddings=(cos, sin)` 传入。
