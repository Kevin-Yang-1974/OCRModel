---
exp_id: GLMOCR-B-260908-008
system: GLMOCR
experiment_type: 确定性回归锚点
date: 2026-09-08
device: BSCC
device_code: B
branch: glm-ocr-layout-ot
job_id: "1483931"
run_id: geometry_seed42_steps256_fp32_lr128_hungarian_det_procfast_v2
mode: geometry
seed: 42
adapter_precision: fp32
layout_loss_profile: full
query_assignment: hungarian
max_steps: 256
lr_schedule_steps: 128
validation_steps: [0, 64, 128, 192, 256]
auxiliary_weight: 0.2
learning_rate: 5.0e-5
warmup_steps: 64
residual_scale_cap: 0.03
processor_mode: fast
processor_class: Glm46VProcessor
image_processor_class: Glm46VImageProcessorFast
processor_backend: torchvision
torch_version: 2.8.0+cu128
transformers_version: 5.3.0
torchvision_version: 0.23.0+cu128
pillow_version: 12.3.0
test_used: false
status: complete
---

# GLMOCR 确定性回归锚点：fast processor / math SDP / Hungarian

## 目的

复核历史 job `1483179` 在 step 256 得到 CER `0.190417` 的结果是否可复现，并把 CUDA/attention 后端、processor 和训练精度显式锁定。该实验只读取 train 与 validation，不读取 test；checkpoint 只由 validation CER 选择。

## 作业与协议

- BSCC job：`1483931`，退出码 `0:0`，正常完成。
- 远端 run：`/home/bingxing2/home/scx9fxd/yangky_ocr_models_bscc_proto/glm_ocr_layout_ot/training_runs/geometry_seed42_steps256_fp32_lr128_hungarian_det_procfast_v2/seed42/geometry_aux0.2/`。
- 训练配置：geometry、seed 42、Hungarian target-slot matching、full layout loss、FP32 adapter/transport/loss、256 steps、LR schedule horizon 128、`auxiliary_weight=0.2`。
- 推理 processor 显式固定为 `Glm46VImageProcessorFast`，backend 为 `torchvision`；固定 validation 页面 fingerprint 与 metadata 一并保存。
- CUDA/attention 路径使用 CuBLAS workspace 配置、math SDP、禁用 Flash/Memory-Efficient SDP，并启用严格 deterministic algorithms。
- `test_used_for_selection=false`；test split 未读取。

## Validation 结果

| checkpoint | CER | exact-page | generation limit | EOS | mean new tokens |
| ---: | ---: | ---: | ---: | ---: | ---: |
| identity / 0 | 0.20763327 | 0 | 0.03125 | 0.96875 | 370.984 |
| 64 | 0.20815184 | 0 | 0.03125 | 0.96875 | 370.797 |
| 128 | 0.20752956 | 0 | 0.03125 | 0.96875 | 370.938 |
| **192** | **0.20685542** | 0 | 0.03125 | 0.96875 | 371.094 |
| 256 | 0.20809998 | 0 | 0.03125 | 0.96875 | 370.547 |

最终按 validation CER 选择 checkpoint `192`，相对 identity 的绝对 CER 改善为 `0.00077785`。所有 checkpoint 的参数与权重均 finite，未发现 NaN/Inf、CUDA error、OOM 或 Traceback。

## 训练与机制诊断

- 训练完成 256 steps；训练阶段平均 OCR loss `1.11229548`、平均 auxiliary loss `3.06389555`、平均 total loss `1.72507460`。
- step 256 的 effective residual scale 为 `0.00147088`，residual relative norm 为 `0.00198252`，均低于预设阈值；invalid transport mass 约 `0.26514`，invalid fusion mass 约 `0.21998`。
- `cer_delta_0_to_64=+0.00051856`，`cer_delta_64_to_128=-0.00062228`；诊断规则判定 dtype mismatch、query mask/no-object、target-slot/auxiliary conflict 和 train/eval dtype mismatch 均未触发。
- 诊断允许从 128 延长到 256；step 192 是最终验证集最佳点，step 256 未继续改善。

## 结果判断

1. 本次显式固定 processor 与确定性 attention/backend 后，validation CER 曲线与 job `1483786` 在 0/64/128/192/256 五个点完全一致，说明当前回归协议可复现。
2. 本次仍未复现 job `1483179` 的 CER `0.190417`；该历史结果不能作为已验证收益或最终回归锚点。结合其非确定性 attention 警告，应将其视为不可复现的异常轨迹。
3. Hungarian matching 下的 64→128 退化得到缓解，但单 seed 的绝对改善幅度较小；这只是确定性机制回归结果，不足以宣称架构收益。

## 后续

- 将本 run 作为确定性 geometry 回归锚点。
- 使用相同的 deterministic/backend/processor 协议重新执行三 seed 的 `content_only`、`attention`、`geometry`、`layout_ot` 统一架构对照；在可复现性通过前，不登记架构排名。
- 架构对照仍只用 validation 选择模式与 checkpoint，协议锁定后再执行 selection-locked test。

## 证据文件

- `metadata.json`：协议、版本、processor 类、资源 hash、代码 hash 与固定页面输入 fingerprint。
- `selection.json`：validation-only checkpoint 选择结果。
- `summary.json`：训练与 validation 汇总。
- `diagnostic_summary.json`：0/64/128/256 机制诊断规则结果。
